# Server configuration
PORT=3001
NODE_ENV=development

# Admin credentials — change this to your Reboot01 username
ADMIN_USERNAME=your_reboot01_username

# Reboot01 API endpoints (don't change these unless the API moves)
REBOOT_SIGNIN=https://learn.reboot01.com/api/auth/signin
REBOOT_GQL=https://learn.reboot01.com/api/graphql-engine/v1/graphql

# CORS — set to your production domain in production
# Development: * (allow all)
# Production:  https://yourdomain.com
ALLOWED_ORIGIN=*
